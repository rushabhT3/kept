# Required only for `kept run --live`. Never commit a real key.
CALLE_API_KEY=calle_live_replace_me
CALLE_BASE_URL=https://api.heycall-e.com

# Second, independent gate. Live calling stays off until this is true.
KEPT_LIVE_CALLS_ENABLED=false
